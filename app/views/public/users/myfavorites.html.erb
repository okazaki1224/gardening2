<p>いいねした投稿一覧</p>
<% @myfavorite_posts.each do |post| %>
<table>
  <tr>
    <td><%= image_tag post.user.profile_image.variant(resize_to_limit:[40,40]).processed %></td>
    <td><%= post.user.nickname %></td>
    <td><%= post.title %></td>
  </tr>
</table>

<% end %>