<%= form_with url: search_posts_path, method: :get, local: true do |f| %>
  <%= f.text_field :keyword, value: keyword %>
  <%= f.submit "検索" %>
<% end %>

タグ：<% tag_lists.each do |tag| %>
  <%= link_to posts_path(tag_id: tag.id) do %>
    #<%= tag.tag_name %>
  <% end %>
<% end %>